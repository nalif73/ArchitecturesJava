version: '3.2'

volumes:
  repiar-app-postgres:
    driver: local

services:
  postgres:
    image: postgres:9.6
    container_name: repiar-app-postgres
    volumes:
      - type: volume
        source: repiar-app-postgres
        target: /var/lib/postgresql/dataConfig
    environment:
      POSTGRES_DB: repiar-app
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgrespass
    ports:
      - "5432:5432"

# jdbc:postgresql://localhost:5432/repiar-app